# 贡献与验证

先读 README 和实现状态，再提出范围明确的变更。代码、配置、文档与可观察验证一起提交。

运行 `python -X utf8 -m unittest discover -s tests -v` 与 `python -X utf8 scripts/check_repository.py`。修改安装器时使用临时 --home，核对用户配置和 state 保留。模型策略变更还需小规模真实配对评测，实际用量未知时标记未知。

提交不包含数据库、安装备份、真实个人路径、秘密或原始附件。来源记录区分阅读深度与复现状态；引用论文结论时不要把其数据当成本项目实测。遇到预算或晋升限制，不通过移除校验使测试变绿。
